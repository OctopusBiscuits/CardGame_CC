using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;
public class scrClickTest : MonoBehaviour
{

    //Im just using this for testing to check that level loading works, this isn't code being used in final game


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void tesst()
    {
        SceneManager.LoadScene(3);
    }
}
